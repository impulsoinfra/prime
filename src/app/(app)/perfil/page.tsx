import { Placeholder } from "@/components/ui/Placeholder";

export default function PerfilPage() {
  return (
    <Placeholder
      titulo="Perfil"
      descripcion="Cuenta, prioridades, frase motivacional y tema (claro/oscuro/sistema). Llega en el Día 2."
    />
  );
}
